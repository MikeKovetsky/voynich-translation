Task: Sentence Structure Analysis (Grammar v2)

We have successfully parsed the text into Roots and Prefixes. The "Word Soup" is gone.
Now we can analyze the sentence structure.

Input:
- results/parsed_text.json

Subtasks:
1. **Reconstruct Sentences:**
   - Read `parsed_text.json`.
   - Group words by line (assuming line = sentence/clause).
   - Represent each word as `Prefix-ROOT`.

2. **N-gram Analysis of Grammar:**
   - What usually follows `qok-`? (e.g. `qok-ee` followed by `al`?).
   - What usually follows `d-`?
   - What starts a line?
   - What ends a line?

3. **Identify Key Verbs:**
   - `qok-` is a strong candidate for a verb prefix.
   - List the top 10 roots that take `qok-`. (e.g., `qok-ee`, `qok-ed`, `qok-ai`).
   - Do these correspond to "Mix", "Take", "Put"?

4. **Identify Key Nouns:**
   - `o-` and `ol-` are strong candidates for Nouns/Articles.
   - List the top 10 roots that take `o-`. (e.g., `o-dy`, `o-ii`).

Output:
- `results/sentence_structure_v2.md`
