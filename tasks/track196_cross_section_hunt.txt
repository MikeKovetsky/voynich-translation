# Task 196: Cross-Section Hunt

## Goal
Find the "Aries Plants" (Nettle, Thistle) in the main Herbal Section to verify the link between Text and Image.

## Input
- `results/parsed_text.json` (Herbal Pages)
- `results/dictionary/dictionary.json`

## Process
1.  **Search:** Find all pages in the Herbal Section (f1r-f66r) where `ald` or `choly` appear as **Labels** (first word/paragraph).
2.  **Visual Verification:**
    - If `choly` appears on Page X, look at the illustration.
    - Does Page X look like a Nettle? (Serrated leaves, hairy stem).
    - If `ald` appears on Page Y, does it look like a Thistle? (Spiky flower).
3.  **Score:**
    - Match / Mismatch / Ambiguous.

## Output
- `results/aries_plant_visuals.md`: Visual verification report.
- `results/track-196-results_summary.md`: Summary.
