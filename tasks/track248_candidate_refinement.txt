Track 248: Candidate Refinement (Descriptive Tags)

Goal: Upgrade the generic `plant_candidate` and `star_candidate` tags in the dictionary to specific descriptive tags using the Adjective Map from Task 247.

Input:
- `results/dictionary/dictionary.json` (v11.0).
- `results/adjective_map.md` (from Task 247).
- `results/mining/plant_candidates.json`.

Logic:
- If `oteody` is frequently followed by `or` (Red), update meaning to `plant_candidate (Red/Hot)`.
- If `qotaiin` is frequently followed by `ol` (Liquid), update meaning to `ingredient_liquid`.

Steps:
1. Load the Dictionary and Candidate lists.
2. Scan the corpus for Adjective pairs.
3. Generate a `dictionary_update_v11_1.json` file with refined meanings.
4. Bump confidence score for refined entries (e.g., from 0.5 to 0.6).
5. Summary: `results/track-248-refinement_report.md`.
