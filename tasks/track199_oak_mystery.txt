# Task 199: The "Oak" Mystery (f9r vs f28v)

## Goal
Resolve the conflict where `choly` (Nettle) appears on f28v (Nettle) AND f9r (Oak/Pansy?).

## Input
- `results/parsed_text.json` (f9r and f28v)
- `results/dictionary/dictionary.json`

## Process
1.  **Compare Text:**
    - Calculate the cosine similarity between the text of f9r and f28v.
    - Do they share other vocabulary?
2.  **Morphology Check:**
    - Is it exactly `choly` on both? Or `choly` vs `chol-y`?
3.  **Hypothesis Testing:**
    - **Same Plant:** f9r is a "bad drawing" of Nettle.
    - **Shared Name:** "Nettle" is a category (e.g., "Stinging Plants").
    - **Misidentification:** f9r is not Oak.

## Output
- `results/choly_investigation.md`: Detailed comparison.
- `results/track-199-results_summary.md`: Summary.
