# Track 329: Translation Cleanup & Deduplication
# Status: Completed
# Priority: High

## Goal
The current translation output is cluttered with duplicate lines (multiple transcribers) and EVA tags (`?1,@P0`). We need to produce a clean, readable version.

## Requirements
1.  **Input:** `translated/THE_VOYNICH_MEDICAL_MANUAL.md`.
2.  **Script:** `research/clean_translation.py`
    -   **Filter:** For each line ID (e.g., `<f10r.1>`), keep ONLY the 'H' transcription (Takahashi). If 'H' is missing, fallback to 'C' or 'F'.
    -   **Strip Tags:** Remove `?1,@P0`, `<$>`, `?`, and other artifacts.
    -   **Format:** Ensure one clean English sentence per Voynich line.
    -   **Output:** `translated/THE_VOYNICH_MEDICAL_MANUAL_CLEAN.md`.

## Expected Output
1.  A readable Markdown file where paragraphs flow naturally.

## Why?
We can't evaluate the literary quality if we are reading 5 versions of the same line.
