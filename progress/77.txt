## Iteration 83: The Visual Verification & Dictionary Expansion 🖼️📚

**Status:** In Progress
**Date:** 2025-11-27

**Goal:**
We reached 15.8% Dictionary Translation Completeness (meaning fully translated words) and 66.5% Confidence. To push these numbers higher (Target: 30% / 80%), we need to:
1.  **Visual Correlation (Track 250):** Systematically link our new "Adjective-Noun" pairs to the illustrations. If we defined `chockhey` as a "Red Plant", does it appear on a page with red paint?
2.  **Suffix Mining (Track 251):** We have mined roots and prefixes. Now we must mine **Suffixes** (`-dy`, `-ol`, `-y`). Many "unknowns" are just known roots with unknown suffixes.
3.  **Hapax Legomena Cleanup (Track 252):** 50% of the dictionary are words that appear only once (Hapax). We will apply "Fuzzy Matching" (Levenshtein distance) to map these to known high-frequency words (correcting transcription errors).

**Tasks Created:**
1.  **Task 250 (Visual Validation):**
    -   Extract "Color Words" from text for each page.
    -   Compare with `images/*.jpg` color histograms (if possible) or manual color tags.
    -   *Hypothesis:* "Red" words (`or`, `otal`) should peak on pages with red ink.
2.  **Task 251 (Suffix Stripping v2):**
    -   Aggressive stripping of `-dy` (adjective marker?) and `-ol` (noun marker?).
    -   If `word - suffix` = `known_root`, inherit the meaning.
3.  **Task 252 (Fuzzy Merger):**
    -   Identify words with freq=1.
    -   Find nearest neighbor with freq>10 (distance <= 1 char).
    -   Merge as "Spelling Variant".

**Hypothesis:**
A significant portion of the remaining "Unknowns" are just spelling variations or grammatical inflections of words we already know. Merging them will skyrocket coverage.
